{
  "name": "hono-react-better-auth",
  "scripts": {
    "server": "bun run --hot server/index.ts",
    "client": "cd client && bun run dev",
    "dev": "concurrently \"bun run server\" \"bun run client\"",
    "db:up": "podman compose up -d",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun run scripts/seed-db-local-scripts.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.15.6",
    "concurrently": "^9.2.1",
    "drizzle-kit": "^0.31.5",
    "drizzle-seed": "^0.3.1"
  }
}